import { SliderHighlightDirective } from './slider-highlight.directive';

describe('SliderHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
